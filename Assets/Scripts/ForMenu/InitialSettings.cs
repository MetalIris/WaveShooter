using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitialSettings : MonoBehaviour
{

    private void Start()
    {

        //if (PlayerPrefs.HasKey("Health") == false)
        {
            //PlayerPrefs.SetInt("Health", 5);
        }
        //if(PlayerPrefs.HasKey("Damage") == false)
        {
           // PlayerPrefs.SetInt("Damage", 1);
        }
        //if(PlayerPrefs.HasKey("GunSpeed") == false)
        {
            //PlayerPrefs.SetInt("GunSpeed",1);
        }
    }
}
